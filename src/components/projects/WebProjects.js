export default function WebProjects() {
  return (
    <div className="w-full h-screen bg-gray-600 flex items-center justify-center overflow-y-scroll">
      {/* Empty full-page scroll area */}
    </div>
  );
}
